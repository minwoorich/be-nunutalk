spring:
  profiles:
    default: local

---

logging:
  level:
    sql: debug
    web: debug
spring:
  config:
    activate:
      on-profile: local

  application:
    name: nunutalk
  datasource:
    url: jdbc:postgresql://43.201.203.85:5432/nunutalk
    username: admin
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    database: postgresql
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
        dialect: org.hibernate.dialect.PostgreSQLDialect

---

spring:
  config:
    activate:
      on-profile: test

  application:
    name: nunutalk
  datasource:
    url: jdbc:h2:mem:db
    username: minwoo
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    database: postgresql
    hibernate:
      ddl-auto: create-drop
#      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100